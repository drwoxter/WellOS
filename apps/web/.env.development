# Local development only (next dev): enables the pasted dev-token sign-in
# form. Production builds never set this; sign-in goes through the
# configured OIDC provider, and the backend independently rejects dev
# tokens unless WELLOS_ENV=development and WELLOS_DEV_AUTH=true.
NEXT_PUBLIC_WELLOS_DEV_AUTH=true
